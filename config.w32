// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("counter", "for counter support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("counter", "enable counter support", "no");

if (PHP_COUNTER != "no") {
	EXTENSION("counter", "counter.c");
}

